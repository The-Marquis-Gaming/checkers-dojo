mod systems {
    mod actions;
}

mod models {
    mod player;
    mod position;
    mod piece;
    mod session;
    mod counter;
    mod coordinates;
    mod winner;
    mod king;
    mod killed;
    mod moved;
}

mod tests {
    mod test_world;
}
